opencv-python         # webcam capture + frame processing
mediapipe            # hand landmark detection
pyobjc-framework-Quartz  # screenshot capture (CGDisplayCreateImage)
pyobjc-framework-Cocoa   # screenshot PNG encoding (NSBitmapImageRep)
sounddevice          # mic capture (PortAudio via sounddevice)
silero-vad           # VAD ONNX model -- voice activity detection
faster-whisper       # STT transcription (CTranslate2, Apple Silicon)
onnxruntime          # ONNX runtime for Silero VAD inference
